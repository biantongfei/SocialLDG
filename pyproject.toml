[project]
name = "SocialSDG"
version = "0.1.0"
description = "Codes of SocialSDG"
authors = [{name="Tongfei Bian"}]
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
